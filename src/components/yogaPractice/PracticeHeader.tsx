import { Box } from "@material-ui/core";

export const PracticeHeader = () => {
  return (
    <Box
      sx={{
        m: 5,
        pt: 2,
        display: "flex",
        justifyContent: "flex-start",
        alignContent: "center",
      }}
    >
      PracticeHeader
    </Box>
  );
};
